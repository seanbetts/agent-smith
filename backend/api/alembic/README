Generic single-database configuration for Agent Smith.

To create a new migration:
    alembic revision --autogenerate -m "description"

To apply migrations:
    alembic upgrade head

To downgrade:
    alembic downgrade -1
