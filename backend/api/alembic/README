Generic single-database configuration for sideBar.

To create a new migration:
    alembic revision --autogenerate -m "description"

To apply migrations:
    alembic upgrade head

To downgrade:
    alembic downgrade -1
